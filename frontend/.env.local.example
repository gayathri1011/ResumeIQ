# Frontend environment — copy to .env.local and fill in values locally.
# Only NEXT_PUBLIC_* variables belong here. Never put secrets in the frontend.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=ResumeIQ
NEXT_PUBLIC_MAX_UPLOAD_SIZE_MB=10
