services:
  mongo:
    image: mongo:7
    container_name: resumeiq-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - resumeiq_mongodata:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  resumeiq_mongodata:
